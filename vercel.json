{
  "buildCommand": "CI=true react-scripts build",
  "outputDirectory": "build",
  "framework": "create-react-app",
  "installCommand": "npm install",
  "build": {
    "env": {
      "REACT_APP_BUILD_TIME": "BUILD_TIMESTAMP"
    }
  }
}
